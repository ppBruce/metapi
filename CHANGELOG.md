# Changelog

本文件记录项目的重要变更。最新条目在上。

---

## 2026-09-13

### 🐛 修复

**修复群组路由中禁用通道仍被使用的问题**

- **问题描述**：在路由群组中手动禁用部分通道后（例如在 claude-opus-5 群组中禁用月城、lanln 通道，只保留林夕通道），系统仍会使用被禁用的通道
- **根本原因**：`tokenRouter.ts` 的 `loadRouteMatch` 函数在查询路由通道时没有过滤 `enabled` 字段
- **修复方法**：在数据库查询的 WHERE 条件中添加 `enabled = true` 过滤
- **影响范围**：
  - 群组路由（explicit_group）
  - 精确路由（exact）
  - 模式路由（pattern/regex）
  - 所有路由策略（按权重、轮询、稳定优先）
- **提交**：`ca4a741`
- **部署状态**：✅ 已部署到生产环境
